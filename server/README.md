# Hidaya API (Node)

Express API: Islamic knowledge JSON, Quran proxy, premium auth (Supabase).

Run locally: `npm ci && node src/index.js` — configure `.env` from `.env.example`.

**Deploy (VPS / Coolify / Flutter):** [DEPLOY.md](DEPLOY.md)
