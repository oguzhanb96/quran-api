# Monorepo: API in /server. Prefer Coolify Base directory "server".
# If the build runs from repo root, these commands apply.

[phases.install]
cmds = ["cd server && npm ci"]

[phases.build]
cmds = ["true"]

[start]
cmd = "cd server && node src/index.js"
